-- Policies pour Supabase Storage
-- Ces policies seront créées via l'interface Supabase ou via des commandes SQL
-- Exemple de structure pour les buckets

-- Bucket pour les documents des ordres de mission
-- CREATE BUCKET IF NOT EXISTS 'documents-ordre-mission' WITH public = false;

-- Bucket pour les documents des projets
-- CREATE BUCKET IF NOT EXISTS 'documents-projets' WITH public = false;

-- Bucket pour les justificatifs de dépenses
-- CREATE BUCKET IF NOT EXISTS 'justificatifs-depenses' WITH public = false;

-- Bucket pour les TdRs
-- CREATE BUCKET IF NOT EXISTS 'tdrs' WITH public = false;

-- Bucket pour les documents GRH
-- CREATE BUCKET IF NOT EXISTS 'documents-grh' WITH public = false;

-- Bucket pour les rapports
-- CREATE BUCKET IF NOT EXISTS 'rapports' WITH public = false;

-- Note: Les policies RLS pour Storage seront configurées via l'interface Supabase
-- ou via des commandes spécifiques selon les besoins de sécurité

